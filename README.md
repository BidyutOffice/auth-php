# AUTH-IN-PHP

pages__
welcome,
dashboard,
profile

register,
login

config__
db
functions

database
users -> id, name, email, password, bio, profile, status("active", "inactive"), createdAt, updatedAt
